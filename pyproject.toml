[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
authors = [
  { email = "30631553+liblaf@users.noreply.github.com", name = "liblaf" },
]
dependencies = []
description = "Add your description here"
name = "mesh-kit"
readme = "README.md"
requires-python = ">= 3.9"
version = "0.0.0"

[tool.hatch.build.targets.wheel]
packages = ["src/mesh_kit"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.rye]
dev-dependencies = []
managed = true
